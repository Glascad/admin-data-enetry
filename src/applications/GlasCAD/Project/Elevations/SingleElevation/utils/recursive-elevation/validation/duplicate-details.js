
export default function elevationContainsNoDuplicateDetails(elevation) {
    // compare all details to each other to make sure none have a matching first container id and second container id
    // if duplicate details found, throw error containing detail ids
}
