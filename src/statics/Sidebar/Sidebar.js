import React from 'react';
import { withRouter } from 'react-router-dom';
import './Sidebar.scss';

function Sidebar() {
    return (
        <div id="Sidebar">
            SIDEBAR
        </div>
    );
}

export default withRouter(Sidebar);