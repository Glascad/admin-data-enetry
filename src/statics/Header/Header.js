import React from 'react';
import { withRouter } from 'react-router-dom';
import './Header.scss';

function Header() {
    return (
        <header id="Header">
            HEADER
        </header>
    );
}

export default withRouter(Header);