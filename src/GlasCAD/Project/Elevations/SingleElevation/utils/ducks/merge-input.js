
export default function mergeElevationInput({
    roughOpening: {
        x,
        y,
    } = {},
    finishedFloorHeight,
    _containerDetails = [],
    _elevationContainers = [],
    sightline = 10,
} = {},
    elevationInput
) {
    return arguments[0];
}
