import React from 'react';

import { Navigator } from '../components';

// import 

export default () => (
    <Navigator
        routes={{
            Authentication,
            Glascad,
            DataEntry,
        }}
    />
);
