export default function getChildren(item, system) {
    const {
        __typename,
        id,
    } = item;

    // if (__typename === "SystemOption"){

    // }else if (__typename === "SystemOptionValue"){

    // }else if(__typename === "DetailOptionValue")
};