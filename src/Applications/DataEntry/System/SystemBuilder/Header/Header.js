import React from 'react';
import { TitleBar } from '../../../../../components';

export default function Header({
    system: {
        
    }
}) {
    return (
        <TitleBar

        />
    );
}