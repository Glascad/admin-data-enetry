import React from 'react';
import {
    Wizard,
} from '../../../components';

export default function SystemOptions() {
    return (
        <Wizard>
            {({ }) => (
                "HELLLO"
            )}
        </Wizard>
    )
}
